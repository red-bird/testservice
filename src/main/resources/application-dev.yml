server:
  port: 8080

spring:
  output:
    ansi:
      enabled: ALWAYS
  application:
    name: testservice
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
#  h2:
#    console:
#      enabled: true
#      path: "/h2-console"

  liquibase:
    enabled: true
  jpa:
#    show-sql: true
    generate-ddl: false
